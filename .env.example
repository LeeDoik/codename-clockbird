# 이 파일을 .env 로 복사한 뒤 실제 키를 넣으세요. (.env 는 git에 커밋되지 않습니다)
# 키 발급: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# 서버 포트 (기본 3000)
PORT=3000

# 개발용: 게임 내에서 백틱(`) 키로 접선 코드(정답)를 볼 수 있게 한다.
# 제출 빌드에서는 이 줄을 지우거나 0 으로 두세요 (정답 비유출 원칙).
REVEAL_ANSWER=1
