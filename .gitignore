node_modules/
dist/
.env
*.local
.DS_Store
Thumbs.db
npm-debug.log*

# check-tile.js 가 뱉는 육안 검수용 미리보기
*.preview4x4.png

# 공유 링크용 (share.bat 이 자동 다운로드)
cloudflared.exe
*.preview8x.png

# 막힌 칸 겹쳐보기 (gen-*-art.js --debug 가 다시 만든다)
src/client/assets/*-blocked-debug.png

# 걷는 길 적용 결과 확인용 (walkmask.js import 가 다시 만든다).
# 칠하는 원본인 design/walkmask/*-walk.png 는 커밋한다 — 그게 충돌의 출처다.
design/walkmask/*-walk-applied.png

# 원본 webp 를 푼 중간 산물 (tools/webp2png.py 가 언제든 다시 만든다).
# 원본 design/맵/**/*.webp 는 커밋한다.
design/맵/_png/

# Claude Code 로컬 설정
.claude/settings.local.json

# 브레인스토밍 목업 서버 작업물
.superpowers/

# 저장소에는 게임 소스와 실행에 필요한 것만 둔다 (2026-08-09).
# 제출 문서·기획 문서·원본 아트·개발 스크립트·도구는 로컬에서만 관리한다 —
# 심사용 공개 저장소를 게임 구동 필수 파일로 한정하기 위해서다.
# README 배너 한 장만 예외로 남긴다.
docs/*
!docs/banner.png
design/
scripts/
tools/
build/
studio.bat
walkmask.bat
share.bat
